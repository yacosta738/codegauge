change: codegauge-distribution
title: "CI/CD and distribution through Cargo, npm, Docker, and release artifacts"
current_phase: verify
completed:
  - init
  - explore
  - propose
  - spec
  - design
  - tasks
  - apply
  - verify
next: qa
updated: "2026-08-15"

baseline:
  repository: yacosta738/codegauge
  remote: git@github.com:yacosta738/codegauge.git
  branch: main
  commit: 6477eb1

summary: |
  Final local verification passed after the Phase 11 stale Release Please fixture repair. The exact Node
  17.6.0 fake-SCM harness, deterministic 0.1.0-to-0.2.0 wrapper fixture, synchronized workspace tests,
  carrier/replay/patch/content/workflow checks, and local distribution gates are green. Only protected
  hosted replay and downstream acceptance remain; no hosted success or external write is claimed.

decision:
  r_f6: option-1-two-stage-version-pr-and-post-merge-tag-carrier
  release_please_version: "17.6.0"
  stage_1_private_boundary: "explicit-five-runtime-cargo-list-plus-root-carrier-four-private-pin-exception"
  stage_1_expected_artifacts: "one synchronized PR with exact private pin update; zero tags and GitHub Releases"
  stage_2_canonical_tag: "exactly one immutable vX.Y.Z at the validated merge SHA"
  private_conformance_exception: "four exact dependency .version JSONPaths only; package version remains 0.1.0 and publish=false"

blocked:
  - Hosted PR #59 failed cargo metadata --locked because private conformance dependency pins remained ^0.1.0
  - Hosted run #31878496886 found the parser rejected GitHub PR-files hunk-only patch context for .release-please-manifest.json
  - Prior Phase 9 verification found a synchronized-tree conformance golden/version failure (remediated locally in apply)
  - Prior Phase 9 verification found filename-only acceptance of a mutated approved generated file (remediated locally in apply)
  - Independent `sdd-qa` acceptance rerun is pending and must wait for a passing fresh `sdd-verify`
  - Corrected private-pin behavior has no hosted verification; do not claim PR #59-equivalent success
  - The hunk-only patch parser fix has no hosted verification; do not claim run #31878496886-equivalent success
  - Hosted Release Please/tag carrier/release workflow rehearsal remains unexecuted
  - Temporary carrier rehearsal guard is implemented locally but requires protected hosted observation
  - Carrier no-match correlation fix is locally verified but not yet hosted-verified; this is separate from the new conformance-pin blocker
  - PAT scope/masking/ref authorization, branch protection, tag delivery, and workflow dispatch recovery remain unconfirmed
  - R-F6 tasks 4.2/7.4/8.4 hosted no-publication rehearsals, task 4.3 QA, and Phase 11 task 11.4 remain downstream gates
  - no v0.1.0 release/tag or GitHub Release exists
  - Cargo/npm/GitHub Release/GHCR publication and attestation not authorized/executed
  - native evidence for seven non-host targets unavailable
  - Fresh technical verification is complete; independent `sdd-qa` acceptance rerun remains pending
  - Hosted/native evidence or an explicit policy exception remains required for archive acceptance

qa:
  verdict: BLOCKED
  report: openspec/changes/codegauge-distribution/qa-report.md
  local_acceptance: fresh technical verification is PASS WITH WARNINGS; independent acceptance and protected hosted replay remain pending; no hosted evidence is claimed
  critical_or_p0_findings: none observed after the fixture repair; hosted acceptance remains pending
  open_p1_findings: hosted lifecycle/provenance, publication/attestation, and native target evidence
